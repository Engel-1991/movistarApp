import { Injectable } from '@angular/core';

export class AuthService {
  estaAutenticado(): boolean {
    // verificar si el usuario está autenticado
    return false; // o false si no está autenticado
  }
}
